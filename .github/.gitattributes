# Set the default behavior to automatically normalize line endings
* text=auto

# Explicitly declare text files you want to always normalize and convert to LF line endings
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Declare files that will always have LF line endings on checkout
*.sh text eol=lf

